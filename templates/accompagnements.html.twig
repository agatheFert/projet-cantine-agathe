{% extends 'base.html.twig' %}

{% block body %}


    <main class="container">
        <h1 class="mt-2">Liste des Plats</h1>
        <section class="row">
            {% for accompagnement in accompagnements %}
                <div class="col-4">
                    <div class="card">
                        <img src="/img/accompagnements/{{ accompagnement.image }}" class="card-img-top"
                             alt="Image de {{ accompagnement.name }}">
                        <div class="card-body">
                            <h2 class="card-title">{{ accompagnement.name }}</h2>
                            <p class="card-text">{{ accompagnement.description }}</p>
                            <a href="#" class="card-link btn btn-outline-info">Détail</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </section>
    </main>


{% endblock %}

{% block title %}

{% endblock %}

